<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- No action bar: the page draws its own header, and a second one above
         it is a second one. The window background is the page's colour so the
         strips beside the status and navigation bars match what they border. -->
    <style name="Theme.HardLines" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/page</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
